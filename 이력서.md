# 이력서

## 하서휘 (Ha Seo-hwi)

**지원 직무: 임베디드·무선통신 시스템 소프트웨어 엔지니어** (Embedded & Wireless Communication SW Engineer)

| 구분 | 내용 |
|------|------|
| 이메일 | apalebluedot9283@gmail.com |
| GitHub | https://github.com/flying-smoothly |
| 학력 | ○○대학교 ○○공학과 (졸업예정) *※ 기재 필요* |

---

## 요약 (Summary)

물리 계층(5G/LTE OFDM)부터 데이터링크 계층(ARQ 프로토콜 스택), 응용 서비스(BLE 실내측위, AI 챗봇)까지 **통신 시스템의 전 계층을 직접 구현해 본 임베디드·무선통신 SW 개발자**입니다.

- STM32(ARM Cortex-M4F) 타겟의 **L2/L3 무선 프로토콜 스택**을 3노드(UE·Anchor·Gateway) 구조로 설계·구현
- 3GPP 표준 기반 **PSS/DMRS 시퀀스 생성과 OFDM 송신 스레드**를 MATLAB 검증 + C 실시간 코드로 구현
- 전통적 FSM 프로토콜 제어를 **DNN 추론으로 대체하는 실험**을 수행하는 등 통신 × AI 융합 역량 보유
- AI 반도체(NPU) 추론, RAG 파이프라인, AWS 클라우드 배포(IaC)까지 **하드웨어–임베디드–서비스를 잇는 풀 스펙트럼** 개발 경험

---

## 핵심 역량 (Skills)

| 분야 | 기술 |
|------|------|
| 임베디드/통신 | C/C++, ARM Cortex-M(STM32 Nucleo-F446RE), mbed OS, Stop-and-Wait ARQ, FSM 설계, 인터럽트/타이머, 시리얼 통신, Arduino |
| 신호처리/PHY | MATLAB, 3GPP LTE/5G 규격(PSS, DMRS, Zadoff-Chu, Gold sequence), OFDM, SIMD 최적화(SSE3), 메모리 정렬 설계 |
| AI/ML | Python, PyTorch 계열 워크플로우, LLM(LLaMA, Claude), RAG(FAISS, SentenceTransformer, LlamaIndex), YOLO, NPU 추론(Rebellions optimum-rbln), NAVER Clova Speech |
| 모바일/웹 | React Native(Expo), TypeScript, Next.js, React, Java/Spring Boot(JPA, Flyway), WebSocket |
| 클라우드/인프라 | AWS(ECS Fargate, RDS, S3, Lambda, Bedrock, CloudFormation/CDK), GitHub Actions CI/CD, Docker, Firebase |
| 협업 | Git/GitHub PR 기반 협업, 3인 팀 프로토콜 공동 개발, AI 페어 프로그래밍 활용 |

---

## 프로젝트 경험

### 1. RSSI 기반 출퇴근 관리 무선 프로토콜 스택 — `network_protocol`
**2026.05 – 2026.06 | 3인 팀 | C++, STM32 Nucleo-F446RE, mbed**

UE·Anchor·Gateway 3노드로 구성된 커스텀 무선 프로토콜 스택을 구현하고, RSSI 임계값 기반 출퇴근(체크인/아웃) 자동 기록 시스템을 완성.

- L2 계층에 **Stop-and-Wait ARQ**를 구현하고, 송수신 시퀀스 번호 충돌 문제를 **txSeqNum/rxSeqNum 분리 설계**로 해결
- 브로드캐스트와 유니캐스트 전송 모드를 분리하고, 브로드캐스트 시 ARQ 중단 버그를 수정해 RSSI PDU 전달 신뢰성 확보
- L3 PDU 타입(MSG/RSSI) 체계를 정의하고, Gateway에서 ID별 출퇴근 시각 로깅·재실자 대상 메시지 중계 기능 구현

### 2. DNN 기반 ARQ 프로토콜 제어 연구 — `protocol2`
**2026.04 | C/C++, Python, STM32, SSE3 SIMD**

데이터링크 계층의 FSM 제어 로직을 **심층신경망(DNN) 추론으로 대체**하는 실험적 프로토콜 스택 구현.

- 이벤트 플래그·상태 비트를 10차원 입력으로 인코딩하고, 출력 뉴런 8개를 임계값 해석해 ARQ 액션(재전송, ACK 처리 등)을 결정하는 추론 루프 설계
- 데스크탑 시뮬레이션용 **SSE3 SIMD 커널**(4-float 병렬)과 임베디드용 스칼라 커널을 동일 API로 이원화, 16바이트 정렬 메모리 관리
- Python 학습 → HDF5 → C 헤더 변환 파이프라인으로 학습 모델을 펌웨어에 정적 링크, `us_ticker_read()`로 추론 지연 실측

### 3. 5G/LTE OFDM 물리 계층 신호 처리 — `bps`
**2026.02 – 2026.03 | MATLAB, C**

3GPP 표준 규격의 동기화·채널 추정용 신호 시퀀스 생성과 OFDM 송신 스레드 구현.

- Zadoff-Chu 수열 기반 **PSS 생성**(N_ID2별 루트 인덱스 선택, 길이 63, 서브캐리어 매핑)과 Gold 시퀀스 기반 **DMRS 생성**을 MATLAB으로 구현·검증
- C 송신 스레드(`vThread_tx`)에서 32바이트 정렬 버퍼, mutex/조건변수 동기화 등 실시간 처리 구조 구현
- rsync/sshpass 자동 배포 스크립트로 연구 서버 반복 빌드·배포 사이클 단축

### 4. BLE 비콘 실내측위 + 긴급 대피 안내 앱 (캡스톤 디자인) — `CAPSTONE`
**2025.10 – 2026.03 | React Native(Expo), TypeScript, Python, WebSocket**

GPS가 닿지 않는 실내에서 BLE RSSI로 위치를 추정하고 화재 시 대피 경로를 안내하는 크로스플랫폼 앱.

- RSSI에 **EMA 필터**(α=0.1)를 적용해 노이즈를 억제하고, 실측 교정값 기반 경로손실 모델(`10^((rssi0−rssi)/20)`)로 거리 추정
- 비콘별 최신 측정값을 1초 주기 배치로 WebSocket 전송, Python 서버에서 **삼변측량** 및 경로 탐색 수행
- CAD 도면 좌표계 → 화면 픽셀 좌표 어파인 변환으로 해상도 무관 정확한 위치 오버레이 구현

### 5. 드론 연계 실내 화재 탐지·대피 유도 시스템 — `drone_searching`
**2025.05 – 2025.11 | Python(asyncio, SciPy), YOLO, React Native, Arduino C**

다층 건물에서 BLE 측위로 재실자를 추적하고, 드론 영상 기반 화재 탐지 시 안전 대피 경로를 실시간 브로드캐스트.

- **YOLO 화재·연기 탐지** 결과를 WebSocket으로 전파하고, 화재 발생 노드를 그래프에서 동적으로 제거하는 **BFS 기반 대피 경로 탐색** 구현
- 최소제곱 최적화(SciPy) 기반 삼변측량으로 층별 86개 구역의 재실자 위치 분류
- MCU(Arduino)에서 경사하강 학습을 수행하는 경량 DNN(C)으로 센서 보정 실험

### 6. AI 반도체 기반 보이스피싱 탐지 금융 챗봇 — `ai-semiconductor-contest`
**2024.10 – 2025.01 | Python, RBLN NPU, LLaMA, FAISS, Clova Speech**

**AI 반도체 기술인재 선발대회 190팀 중 최종 5팀 선발, 2024 인공지능반도체 미래기술 컨퍼런스 발표.**

- Rebellions NPU SDK(`optimum-rbln`)로 LLaMA 모델을 컴파일·배포하고, 컴파일 캐싱으로 초기화 비용 절감
- FAISS + SentenceTransformer 임베딩으로 유사 보이스피싱 사례를 검색하는 **RAG 파이프라인** 구축, 환각 억제 및 탐지 신뢰도 향상
- Clova Speech STT(실시간/단문/장문) 연동, 슬라이딩 윈도우 + 요약 히스토리 병행 방식의 대화 맥락 관리 설계

### 7. 휴대폰 판매점 통합 웹 플랫폼 + AWS 인프라 자동화 — `biz_way_AWS`
**2026.05 – 2026.06 | Next.js 16, React 19, PostgreSQL(Prisma), AWS CDK**

4개 판매점 사이트를 통합한 B2B 플랫폼(약 23K LOC)을 개발하고 AWS 배포를 코드로 완전 자동화.

- ECS Fargate + RDS + ALB + Secrets Manager 구성을 **CDK(IaC)** 로 선언, GitHub Actions OIDC 기반 CI/CD 구축
- 월 예산 초과 시 Lambda가 ECS를 자동 중지하는 **비용 자동 차단(Budget auto-stop)** 및 원클릭 전체 리소스 삭제 워크플로우 구현
- 실시간 단말기 가격 계산기(공시지원금/선택약정, 할부이자 5.9% 자동 계산), JWT 인증, 역할 기반 접근 제어

### 8. 통신 유통 정산 관리 시스템 — `rail_way`
**2026.05 | Java 17, Spring Boot 3.3, JPA, MySQL, Flyway, Apache POI**

통신 유통 채널의 도매정산·판매일지 업무를 위한 스프레드시트형 웹 정산 플랫폼.

- 119개 필드 판매 트랜잭션 엔티티, 월별 동적 컬럼 헤더(JSON 컬럼) 등 변화가 잦은 업무 규칙에 대응하는 스키마 설계
- 셀 단위 실시간 편집(AJAX)에서 발생한 저장 경합을 **프로미스 큐 직렬화**로 해결, Hibernate 배치 INSERT/IN-clause 배칭으로 N+1 방지
- Flyway 마이그레이션 18개 버전 운영, Docker 멀티스테이지 빌드, 세션 제한·감사 로깅 등 보안 하드닝

### 9. 자동 맞춤형 화장품 성분 배합 시스템 — `red_show`
**2025.02 | Python(pyserial, firebase-admin), Arduino C++**

Firebase의 맞춤 처방 데이터를 읽어 스테퍼 모터 전동 피펫을 제어하는 HW-SW 통합 자동화 시스템.

- 50μl당 200스텝의 물리 교정 상수 기반 스텝 계산, 최대 용량 초과분 반복 배분 로직 구현
- 모터 드라이버 ENABLE 핀 제어로 대기 시 발열·전류 소비 절감, 블루투스/유선 시리얼 이중 지원

### 10. 캠퍼스타운 해커톤 — AI 맛집 추천 챗봇 — `campus_town_hackerton`
**2024.11 – 2025.01 | Python, Streamlit, AWS Bedrock(Claude), LangChain, Lambda**

- Google Places API + AWS Rekognition + Claude 리뷰·이미지 분석을 결합한 다단계 추천 파이프라인 구축
- 자연어에서 사용자 조건(음식 종류·예산·분위기)을 추출하고 LlamaIndex 벡터 인덱싱으로 시맨틱 검색 구현

---

## 수상 및 활동

- **AI 반도체 기술인재 선발대회 최종 5팀 선발** (190팀 참가) — 2024
- **2024 인공지능반도체 미래기술 컨퍼런스 성과 발표** — 2024
- 캠퍼스타운 해커톤 참가 (AI 챗봇 개발) — 2024
- 캡스톤 디자인: BLE 실내측위 긴급 대피 안내 시스템 — 2025–2026

---

*※ 학력, 어학, 자격증 등 인적 사항은 확인된 정보가 없어 비워두었습니다. 필요 시 채워 넣어 주세요.*
