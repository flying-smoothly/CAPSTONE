# 자기소개서

**지원 직무: 임베디드·무선통신 시스템 소프트웨어 엔지니어**
**하서휘 (apalebluedot9283@gmail.com / github.com/flying-smoothly)**

---

## 1. 지원 동기 및 직무 역량 — "물리 계층부터 응용 서비스까지, 통신의 전 계층을 직접 만들어 봤습니다"

저는 통신 시스템을 '쓰는 사람'이 아니라 '만드는 사람'이 되고 싶어 학부 시절 내내 프로토콜 스택의 아래층부터 위층까지 한 층씩 직접 구현해 왔습니다.

물리 계층에서는 3GPP 표준 문서를 따라 Zadoff-Chu 수열 기반 PSS와 Gold 시퀀스 기반 DMRS 생성 알고리즘을 MATLAB으로 구현·검증하고, 32바이트 정렬 버퍼와 mutex 동기화를 적용한 C 송신 스레드로 연동했습니다. 데이터링크 계층에서는 STM32(ARM Cortex-M4F) 보드 위에 Stop-and-Wait ARQ 프로토콜을 FSM으로 구현했고, 응용 계층에서는 BLE RSSI 기반 실내측위 시스템을 모바일 앱부터 측위 서버까지 풀스택으로 완성했습니다.

표준 문서의 수식 한 줄이 실제 하드웨어에서 동작하는 신호가 되기까지의 전 과정을 경험하면서, 저는 통신 SW 개발의 본질이 '계층 간 인터페이스를 정확히 이해하고 끝까지 디버깅하는 집요함'이라는 것을 배웠습니다. 이 경험을 바탕으로 차세대 통신 시스템 개발에 기여하고 싶습니다.

## 2. 문제 해결 경험 — "시퀀스 번호 하나가 안 맞아 통신이 무너졌을 때"

3인 팀으로 진행한 무선 출퇴근 관리 시스템 프로젝트에서, UE·Anchor·Gateway 3개 노드 간 통신이 간헐적으로 끊기는 문제를 만났습니다. 커밋 로그에 "seqNum 안 맞는 문제 발생"이라고 적어둘 만큼 원인을 특정하기 어려운 버그였습니다.

저는 추측 대신 PDU 덤프를 노드별로 출력해 시퀀스 번호의 흐름을 추적했고, 하나의 seqNum 변수를 송신과 수신이 공유하면서 ARQ 상태가 꼬인다는 근본 원인을 찾았습니다. 이를 txSeqNum/rxSeqNum으로 분리하는 구조 변경으로 해결했고, 이어서 브로드캐스트 수신 시 ARQ를 중단해 버리는 두 번째 버그, 노드 간 PDU 포맷 불일치라는 세 번째 버그까지 연쇄적으로 잡아내며 시스템을 안정화했습니다. SN 불일치 시 PDU를 버리는 대신 rxSeqNum을 동기화하도록 복구 로직도 개선했습니다.

이 경험으로 저는 프로토콜 버그는 한 번에 하나씩, 계층을 분리해서, 가설이 아닌 로그로 잡아야 한다는 디버깅 원칙을 체득했습니다. 화면에 에러 메시지가 뜨지 않는 임베디드 환경에서도 끝까지 원인을 추적하는 끈기가 저의 가장 큰 강점입니다.

## 3. 차별화 역량 — "통신과 AI의 경계에서 새로운 시도를 합니다"

저는 통신 프로토콜과 AI를 각각 아는 것을 넘어, 두 영역을 결합하는 실험을 해 왔습니다.

ARQ 프로토콜의 FSM 제어 로직을 DNN 추론으로 대체하는 프로젝트에서는, 이벤트 플래그와 상태 비트를 10차원 입력 벡터로 인코딩하고 출력 뉴런 8개를 임계값으로 해석해 재전송·ACK 처리 같은 프로토콜 액션을 신경망이 결정하게 했습니다. 데스크탑에서는 SSE3 SIMD로 4-float 병렬 추론 커널을, 임베디드 타겟에서는 동일 API의 스칼라 커널을 구현해 환경별 최적화를 분리했고, 추론 지연을 마이크로초 단위로 실측해 FSM 대비 실현 가능성을 평가했습니다.

또한 190팀이 참가한 AI 반도체 기술인재 선발대회에서 NPU에 LLaMA 모델을 컴파일·배포하고 FAISS 기반 RAG로 보이스피싱을 탐지하는 챗봇을 구현해 최종 5팀에 선발되었고, 인공지능반도체 미래기술 컨퍼런스에서 성과를 발표했습니다. 통신 인프라가 AI와 융합되는 흐름 속에서, 양쪽 언어를 모두 구사하는 엔지니어로서 기여할 수 있다고 자신합니다.

## 4. 협업 경험과 입사 후 포부 — "코드 리뷰와 작은 커밋으로 함께 만드는 개발"

무선 프로토콜 프로젝트는 팀원 3명이 UE·Anchor·Gateway를 나누어 개발했기 때문에, 인터페이스 합의가 곧 생존이었습니다. 저희는 PDU 타입 정의를 공통 헤더로 통일하고, GitHub PR 단위로 변경을 검토하는 워크플로우를 유지했습니다. 그 결과 3주라는 짧은 기간에 60회 이상의 커밋을 쌓으며 동작하는 시스템을 완성할 수 있었습니다.

웹 플랫폼 프로젝트(biz_way, rail_way)에서는 100개가 넘는 PR을 작은 단위로 쪼개어 머지하는 습관을 들였고, AI 코딩 도구를 페어 프로그래머로 활용해 진단 로그 추가 → 원인 확정 → 수정 → 로그 제거의 체계적인 사이클로 버그를 잡는 방법을 익혔습니다. 빠르게 도구를 받아들이되, 최종 판단과 검증은 엔지니어가 책임진다는 원칙을 지켰습니다.

입사 후에는 먼저 담당 시스템의 프로토콜과 코드베이스를 누구보다 깊이 파악하는 엔지니어가 되고, 나아가 물리 계층 신호처리부터 AI 기반 최적화까지 아우르는 저의 스펙트럼을 살려 차세대 통신 시스템의 지능화에 기여하겠습니다.

---

*본 문서는 GitHub 리포지토리 10개의 커밋 이력과 코드를 근거로 작성되었습니다. 학교·전공 등 인적 사항과 지원 회사명은 지원처에 맞게 수정해 주세요.*
